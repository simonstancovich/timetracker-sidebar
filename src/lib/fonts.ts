import { fontFamily } from "../theme/typography";

// Aliases for inline styles, re-exported from the design-system typography
// contract so legacy inline-styled code and the primitives share one stack.
export const MONO = fontFamily.mono;
export const SERIF = fontFamily.display;
